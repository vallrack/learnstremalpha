import AdminPodcastsClient from './AdminPodcastsClient';

export const maxDuration = 60; // Para permitir subidas largas si es necesario

export default function AdminPodcastsPage() {
  return <AdminPodcastsClient />;
}
